import React, { Style } from 'react';
import api from '../../services/api';

import '../../../src/App.css';

export default function Contagem() {
  return (
    <div className="containerContagem">
      <div className="contentContagem">
      </div>
    </div>
  )
}